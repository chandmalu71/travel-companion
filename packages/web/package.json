{
  "name": "@travel-companion/web",
  "version": "0.1.0",
  "private": true,
  "description": "Next.js web application",
  "scripts": {
    "build": "next build",
    "dev": "next dev",
    "start": "next start",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:ci": "vitest run --coverage",
    "clean": "rm -rf .next out"
  },
  "dependencies": {
    "@travel-companion/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
